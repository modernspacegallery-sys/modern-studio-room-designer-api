{
  "name": "modern-studio-room-designer-api",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless function that proxies room redesign requests to OpenAI, keeping the API key server-side.",
  "engines": {
    "node": ">=18"
  }
}
